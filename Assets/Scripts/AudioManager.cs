using UnityEngine;

public class AudioManager : MonoBehaviour
{
    [SerializeField] private AudioSource Music;

    [SerializeField] private AudioSource ButtonPress;

    [SerializeField] private AudioSource EventPopup;

    [SerializeField] private AudioSource NextTurn;

    [SerializeField] private AudioSource LearnHabit;
    
    [SerializeField] private AudioSource LevelFail;
    
    [SerializeField] private AudioSource LevelSuccess;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
